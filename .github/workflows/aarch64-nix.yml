---
name: NixOS aarch64-linux workflow

on:  # yamllint disable-line rule:truthy
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: [Linux, ARM64, nix]

    steps:
      - uses: actions/checkout@v2

      - name: run
        run: nix run
